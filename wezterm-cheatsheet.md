# WezTerm Keybind Cheatsheet

**Leader Key**: `Ctrl + Space` (timeout: 2秒)
**検索方法**: `Ctrl + F` でキーワード検索

---

## Quick Start

最小限覚えておくべき操作：

- `Leader` = `Ctrl + Space`
- **Pane分割**: `Leader` → `r` (上下) / `d` (左右)
- **Pane移動**: `Leader` → `h` `j` `k` `l`
- **Pane閉じる**: `Leader` → `x`
- **Tab新規**: `Leader` → `t`
- **Tab切替**: `Ctrl + Tab` / `Ctrl + Shift + Tab`
- **Tab閉じる**: `Leader` → `c`
- **終了**: `Alt + F4`
- **コピーモード**: `Leader` → `[` → vim操作 → `y` でコピー → `q` で終了
- **コマンドパレット**: `Leader` → `p` または `Ctrl + Shift + p`

---

## Core / Global

### コピー & ペースト
- `Ctrl + Shift + c` - クリップボードにコピー
- `Leader` → `v` - クリップボードから貼り付け
- `Ctrl + Shift + v` - クリップボードから貼り付け

### 画面・表示
- `Alt + Enter` - フルスクリーン切り替え
- `⌘ +` - フォントサイズ拡大
- `⌘ -` - フォントサイズ縮小
- `⌘ 0` - フォントサイズを設定ファイルの値にリセット

### システム
- `Leader` → `p` - コマンドパレット表示
- `Ctrl + Shift + p` - コマンドパレット表示
- `Ctrl + Shift + r` - 設定ファイル再読み込み

---

## Leader Key Overview

**Leader後に続けて押すキー一覧**

| キー | 動作 |
|------|------|
| `w` | ワークスペース切り替え |
| `W` (Shift) | 新規ワークスペース作成 |
| `$` | ワークスペース名前変更 |
| `t` | 新規Tab作成 |
| `c` | Tab閉じる |
| `{` | Tab左移動 |
| `}` | Tab右移動 |
| `1` ~ `9` | Tab番号で直接切り替え |
| `p` | コマンドパレット |
| `v` | ペースト |
| `r` | Pane横分割 |
| `d` | Pane縦分割 |
| `x` | Pane閉じる |
| `h` `j` `k` `l` | Pane移動 (←↓↑→) |
| `z` | Pane最大化/戻す |
| `s` | Paneリサイズモード起動 |
| `a` | Pane移動モード起動 |
| `[` | コピーモード起動 |

**Note**: `s` はリサイズモード起動（Enter で終了）/ `a` は移動モード起動（約1秒でタイムアウト）

---

## Pane Operations

### Pane分割
- `Leader` → `r` - 上下に分割（下にペイン作成）
- `Leader` → `d` - 左右に分割（右にペイン作成）

### Pane移動（直接）
- `Leader` → `h` - 左のPaneへ
- `Leader` → `j` - 下のPaneへ
- `Leader` → `k` - 上のPaneへ
- `Leader` → `l` - 右のPaneへ

### Pane選択・操作
- `Ctrl + Shift + [` - Pane選択モード（番号で選択）
- `Leader` → `x` - 現在のPaneを閉じる（確認あり）
- `Leader` → `z` - 現在のPaneを最大化/元に戻す

### Paneリサイズモード
**起動**: `Leader` → `s`

モード内の操作：
- `h` - 左方向に縮小
- `j` - 下方向に拡大
- `k` - 上方向に拡大
- `l` - 右方向に拡大
- `Enter` - モード終了

### Pane移動モード
**起動**: `Leader` → `a` (1秒でタイムアウト)

モード内の操作：
- `h` - 左のPaneへ
- `j` - 下のPaneへ
- `k` - 上のPaneへ
- `l` - 右のPaneへ

---

## Tab Operations

### Tab作成・削除
- `Leader` → `t` - 新規タブ作成
- `Leader` → `c` - 現在のタブを閉じる（確認あり）

### Tab切り替え
- `Ctrl + Tab` - 次のタブへ
- `Ctrl + Shift + Tab` - 前のタブへ
- `Leader` → `1` ~ `8` - タブ番号で直接指定
- `Leader` → `9` - 最後のタブへ

### Tab移動（並び替え）
- `Leader` → `{` - タブを左に移動
- `Leader` → `}` - タブを右に移動

---

## Workspace

- `Leader` → `w` - ワークスペース切り替え（一覧表示）
- `Leader` → `W` (Shift+W) - 新規ワークスペース作成（名前入力）
- `Leader` → `$` - 現在のワークスペース名変更

---

## Key Tables / Modes

### 利用可能なモード一覧
1. **resize_pane** - Paneサイズ調整
2. **activate_pane** - Pane連続移動
3. **copy_mode** - テキスト選択・コピー

### モード状態の確認
- 右上ステータスエリアに `TABLE: モード名` が表示される

---

## Copy Mode

**起動**: `Leader` → `[`

### 移動（基本）
- `h` `j` `k` `l` - 左・下・上・右
- `w` - 次の単語へ
- `b` - 前の単語へ
- `e` - 単語の末尾へ

### 行内移動
- `0` - 行頭へ
- `^` - 行頭（空白以外）へ
- `$` - 行末へ

### ページ・画面移動
- `Ctrl + f` - 1ページ下へ
- `Ctrl + b` - 1ページ上へ
- `Ctrl + d` - 半ページ下へ
- `Ctrl + u` - 半ページ上へ

### スクロールバック移動
- `g` - スクロールバック最上部へ
- `G` - スクロールバック最下部へ

### ビューポート移動
- `H` - 画面上部へ
- `M` - 画面中央へ
- `L` - 画面下部へ

### ジャンプ機能
- `f` → `<char>` - 行内で指定文字に前方ジャンプ
- `F` → `<char>` - 行内で指定文字に後方ジャンプ
- `t` → `<char>` - 指定文字の直前に前方ジャンプ
- `T` → `<char>` - 指定文字の直前に後方ジャンプ
- `;` - 直前のジャンプを繰り返す

### 選択モード
- `v` - セル選択モード
- `V` - 行選択モード
- `Ctrl + v` - ブロック選択モード
- `o` - 選択範囲の反対端に移動
- `O` - 選択範囲の反対端（水平）に移動

### コピー・終了
- `y` - 選択範囲をクリップボードにコピー
- `Enter` - 選択範囲をコピーしてモード終了
- `Escape` - モード終了
- `Ctrl + c` - モード終了
- `q` - モード終了

---

## View / Font

### フォントサイズ変更（キーバインド）
- `⌘ +` - フォントサイズ拡大（`IncreaseFontSize`）
- `⌘ -` - フォントサイズ縮小（`DecreaseFontSize`）
- `⌘ 0` - フォントサイズを設定ファイルの値にリセット（`ResetFontSize`）

### デフォルト設定
- フォント: `UDEV Gothic 35NFLG`
- サイズ: `12.0`
- 定義場所: `wezterm.lua` (`config.font` / `config.font_size`)

### 仕組み・注意点
- `⌘ +` / `⌘ -` によるサイズ変更は**そのウィンドウだけの一時的な上書き**で、`wezterm.lua` は書き換わらない
  - → 何度も押して小さく/大きくなりすぎた場合は `⌘ 0` で一発リセットできる
  - → ウィンドウを閉じて開き直しても直る（新規ウィンドウは設定ファイルの値からスタート）
- サイズを**恒久的に**変えたい場合は `wezterm.lua` の `config.font_size` を直接編集して保存する（`automatically_reload_config = true` のため保存すれば即反映）
- nvim・シェルなどペイン内のアプリは、WezTerm自体が描画しているグリフをそのまま表示しているだけなので、個別にフォント設定を持たない
  - → nvimだけ文字が小さい/大きいと感じた場合も、原因はWezTerm側のフォントサイズであることがほとんど

### 画面表示
- `Alt + Enter` - フルスクリーン切り替え

---

## Troubleshooting / Notes

### 注意事項
- デフォルトキーバインドは無効化されています（`disable_default_key_bindings = true`）
- Leader keyのタイムアウトは2秒
- WezTerm 終了用のキー割当は未定義のため、`Alt + F4` が確実

### よくある操作パターン
1. **Paneレイアウト調整**: `Leader` → `s` → `hjkl` 連打 → `Enter`
2. **素早くコピー**: `Leader` → `[` → 移動 → `v` → 選択 → `y` → `q`
3. **ワークスペース切替**: `Leader` → `w` → 矢印キーで選択 → `Enter`

### トラブル時
- 設定がおかしい → `⌘ R` で再読み込み（`ReloadConfiguration`）
- 文字が急に小さく/大きくなった → `⌘ 0` でフォントサイズをリセット（[View / Font](#view--font) 参照）
- 全機能にアクセス → `Leader` → `p` でコマンドパレット
